/**
 * @deprecated Brug `@/lib/wine-detail-pages/registry` — beholdt som tynd re-export.
 */
export {
  dsfPopularWineDetailSlugForProductUrl,
  getDsfPopularWineBySlug,
  listDsfPopularWineSlugs,
  popularWineToFeaturedPick,
  type DsfPopularWineAside,
  type DsfPopularWineFoodPairing,
  type DsfPopularWineGuideRef,
  type DsfPopularWinePage,
  type DsfPopularWineSpec,
} from "@/lib/wine-detail-pages/registry";

export { sanitizeDsfProductUrl } from "@/lib/dsf-product-url";
